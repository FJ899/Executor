---
document: "Executor Human interaction contract pointer"
status: "ACTIVE POINTER / NOT SEMANTIC OWNER"
date: "2026-08-21"
semantic_owner: "HUMAN"
canonical_repository: "FJ899/Saddle"
canonical_path: "docs/HUMAN_OPERATING_CONTRACT.md"
live_resolution: "RESOLVE CURRENT SADDLE MAIN AT READ TIME"
---

# Human interaction contract — current pointer

Executor does not own the Human-facing collaboration contract. The Human is the semantic owner; Saddle is the durable canonical repository for its current copy.

Current Human-facing surface:

```text
AKCJA
GDZIE
ODESŁAĆ
```

Canonical durable source:

```text
FJ899/Saddle
docs/HUMAN_OPERATING_CONTRACT.md
```

Resolve the current Saddle `main` at read time if the full contract is required. Do not use a historical Executor protocol as a substitute for the current Human-owned contract.

This pointer does not create effect authority, implementation authority, product direction, runtime behavior, or a new ownership relation.

```text
CAPABILITY != PERMISSION
PROPOSAL != DECISION != AUTHORITY != EFFECT
```
