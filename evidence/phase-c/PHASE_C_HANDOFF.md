# Independent Phase C Handoff

## Status

`PHASE B IMPLEMENTATION CANDIDATE / REAL DIRECT-HUMAN AUTHORITY EVENTS + TWO REAL DRAFT-PR PILOT RESULTS OBSERVED / P4 NOT CLAIMED`

The executing agent is not the independent verifier. This handoff intentionally does not hard-code the final Executor candidate SHA: the verifier must resolve the current head of `JTJ07/Executor#61` and accept only evidence generated by a successful exact-head workflow run for that same SHA.

## Human-selected constants

- DONE: P4 Repeatable Executor 1.0.
- Trusted front door/provider: external GitHub intake.
- Allowed actor/profile: `trust_profiles/github-p4-pilots.json`.
- Solution ownership: external Intelligence.
- Pilot repositories: `JTJ07/scriptops` and `JTJ07/creative-os-project-reconstructor`.
- Effects: branch, commit and draft PR only.
- Forbidden: merge, deploy, release, new secrets and paid services.

## Candidate implementation

- Executor draft PR: `JTJ07/Executor#61`; resolve and pin its current exact head/tree at verification time;
- current GitHub issue/comment and target commit/tree verification;
- exact draft hash and `ACCEPT/MODIFY/REJECT` handling;
- durable SQLite authority consumption and terminal result binding;
- authority-free external solution candidate/proposal interface;
- bounded pilot sandbox runtime and draft-PR request artifact;
- adversarial, concurrency, proposal and runtime tests;
- exact-head dual-pilot replay workflow that must preserve the SQLite ledger in uploaded evidence.

Historical workflow runs remain useful provenance only. They do not substitute for the exact completion-candidate evidence chain.

## Real governed inputs and outputs already observed

- Request issue `JTJ07/Executor#62`: ScriptOps numeric candidate ordering, exact source `daa6e5dc210e09171a530eeffe5601e0e74ae041` / tree `ba0b99bd99682dcc7a942537b502a7a72151fa09`.
- Request issue `JTJ07/Executor#63`: Reconstructor symlink boundary, exact source `defc7b029097284f94136fec54b75c313ac12f68` / tree `5960d74773b19456863ac66084385b7619aa4285`.
- Real direct-human ACCEPT comments exist in both issues. Every replay must use a fresh non-edited ACCEPT and may not reuse an expired/replayed decision.
- ScriptOps governed result: `JTJ07/scriptops#8`, open draft PR, base `daa6e5dc...`, result head `897de878703a029df814f2551b993c3818defa2a`, exactly one production file changed.
- Reconstructor governed result: `JTJ07/creative-os-project-reconstructor#4`, open draft PR, base `defc7b02...`, result head `e59b9d6c1b496bcb6411e712e7c65cc891578ac3`, exactly one production file changed.
- Both target repositories have successful CI on those result heads.

The pilot PRs are review artifacts only. Their existence does not authorize merge and does not satisfy the endpoint-value/human-review gate by itself.

## Exact-candidate evidence rule

For each pilot, Phase C must locate a successful workflow run whose `head_sha` equals the current PR #61 head and independently inspect its artifact. The artifact must contain at minimum:

- frozen contract JSON;
- authority-free materialized proposal JSON;
- terminal result JSON;
- binary-safe patch;
- target status;
- `.executor/*-authority.sqlite3` durable ledger (and SQLite companion files if present);
- run evidence directory;
- exact workflow/sandbox/source identities.

The verifier must inspect the actual SQLite state and recompute/compare durable origin-to-result bindings instead of trusting the terminal JSON self-report.

## Independent verification obligations

At the current exact candidate SHA, independently rerun G-01 through G-18 from `PROJECT_COMPLETION_MAP.md`, including:

- full forged/wrong-actor/edited/replayed/expired matrix;
- Docker isolation and cleanup;
- atomic race, initialization race and crash state;
- package installation and current required CI;
- exact source/workflow/sandbox identity for both real pilots;
- complete evidence replay from raw artifacts including the SQLite ledgers;
- human review/value/time/cost measurements required by endpoint C;
- repository closure/archival state;
- documentation-state consistency;
- final human acceptance boundary.

Return exactly one verdict:

```text
PROJECT COMPLETION: PASS
PROJECT COMPLETION: BLOCKED
PROJECT COMPLETION: FALSE-COMPLETION
```

P4 remains unclaimed until all applicable gates pass. Only an explicit human decision may accept Executor 1.0 or authorize any implementation/pilot merge, release or deployment.
