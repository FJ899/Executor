name: GP001 product execution recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Automatic execution recovery replay disabled.'
          echo 'Attempted run: 33006665936.'
          echo 'Inspect exact preserved artifacts and authority state before any further execution.'
