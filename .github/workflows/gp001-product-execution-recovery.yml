name: GP001 product execution recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Current one-shot execution workflow is disarmed.'
          echo 'Intended current execution run: 33047492859.'
          echo 'Do not replay automatically; inspect authority consumption before any recovery.'
