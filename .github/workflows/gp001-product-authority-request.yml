name: GP001 product authority request renewal one-shot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Automatic fresh authority request renewal disabled.'
          echo 'Run 33013131624 formed fresh request gp001-product-authority-e2e-002.'
          echo 'GitHub created Issue #90 before CLI JSON serialization failed.'
          echo 'Do not repeat CREATE_ISSUE; reconcile and use Issue #90 as zero-authority transport.'
