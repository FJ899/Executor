name: GP001 product draft PR publication one-shot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Automatic draft PR publication replay disabled.'
          echo 'Credential validation attempt: run 33009929226.'
          echo 'The target checkout failed before publish-draft-pr and before any target provider write.'
          echo 'Replace EXECUTOR_GITHUB_EFFECT_TOKEN with a real PAT scoped to FJ899/executor-pilot-target before any explicitly re-armed retry.'
