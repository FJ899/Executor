name: GP001 product post-human one-shot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Automatic replay disabled after first direct-human ACCEPT.'
          echo 'Historical attempted runs: 33005720327 and 33005980771.'
          echo 'Continue only through explicit recovery/correction flow; never blind-retry target effects.'
