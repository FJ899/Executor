name: GP001 product draft PR publication one-shot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Automatic draft PR publication replay disabled.'
          echo 'Credential-only attempt: run 33009929226 failed before publish-draft-pr.'
          echo 'Credential-corrected attempt: run 33011126247 entered publish-draft-pr and failed.'
          echo 'Treat current state as potential partial side effect; reconcile receipts and provider state before any retry.'
