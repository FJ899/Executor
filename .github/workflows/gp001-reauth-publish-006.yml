name: GP001 reauthorized draft PR recovery fix one-shot — DISARMED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disarmed:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Historical one-shot boundary only; no replay.'
