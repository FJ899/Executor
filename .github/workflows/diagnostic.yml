name: Chat GP001 Issue 97 decision one-shot disarmed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disarmed:
    runs-on: ubuntu-24.04
    if: ${{ false }}
    steps:
      - run: echo disarmed
