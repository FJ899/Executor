name: GP001 draft publication retry plan zero effect — disarmed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disarmed:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Retry-plan workflow is disarmed.'
          echo 'Canonical retry-plan run: 33048311105.'
          echo 'Artifact: 9636522963.'
