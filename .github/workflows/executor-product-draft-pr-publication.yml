name: Executor product draft PR publication

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-boundary:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Current one-shot draft PR publication is disarmed.'
          echo 'Intended publication run: 33047770719.'
          echo 'Do not replay automatically; reconcile receipt/provider state before any recovery.'
