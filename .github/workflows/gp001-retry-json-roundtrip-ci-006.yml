name: GP001 retry JSON round trip verification 006 — DISARMED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disarmed:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Historical verification boundary only.'
