name: GP001 reauthorized publication read-only forensics — DISARMED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disarmed:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Historical read-only diagnostic boundary only.'
